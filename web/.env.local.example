# Base URL of the FastAPI tutor backend. Trailing slashes are trimmed.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional API key. When set, it is sent as the `X-API-Key` header on every
# request. Leave empty when the backend runs without authentication.
NEXT_PUBLIC_API_KEY=
